services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - uploads:/app/uploads
    environment:
      DATABASE_URL: sqlite:///./clinical_intake.db
      UPLOAD_DIR: ./uploads
      MIN_TEXT_CHARS: 50
      LOW_CONFIDENCE_THRESHOLD: "0.5"

volumes:
  uploads:
